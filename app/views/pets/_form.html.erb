<%= simple_form_for pet do |f| %>
  <%= f.input :category, placeholder: 'Dog, Cat,...' %>
  <%= f.input :date, label: 'Found on' %>
  <%= f.input :description %>
  <%= f.input :address %>

  <%= f.submit 'submit', class: 'btn btn-primary w-100' %>
<% end %>
